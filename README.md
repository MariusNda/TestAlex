# Vault CASA v3 — GT Architectures Réglementaires · Cyber Resilience Act

Architecture 3 couches : SOURCES (PageIndex : texte CRA indexé ✅) · CONNAISSANCE (ce vault) ·
MÉTHODE (skills + commandes). Colonne vertébrale : la chaîne de traçabilité
TEXTE → EXIGENCES → CINÉMATIQUES → CARTO → FAITS/HYPOTHÈSES → ARBRES → CONCLUSIONS → SLIDES.

## Démarrage
1. Ouvrir Claude Code à la racine (lit CLAUDE.md automatiquement)
2. MCP : PageIndex ✅ (CRA indexé) · Notion (`claude mcp add --transport http notion https://mcp.notion.com/mcp`) · Outlook+To Do (softeria, voir ci-dessous)
3. Skills NDA utilisés en complément : drawio-diagram (conversion Mermaid → .drawio), pptx (slides)

## Commandes
`/loi` (question juridique sourcée) · `/exigence` (décliner le texte en CRA-EX) ·
`/reunion` (AI meeting notes Notion → CR Notion + vault)

## Séquence de lancement CRA
1. Phase 0 : entités contributrices + études → sources.md ; rétroplanning → journal.md
2. Instruire CRA-H01 (CRA × DORA, statut fabricant) : conditionne tout le périmètre
3. `/loi` sur les jalons calendaires ; `/exigence` domaine par domaine
4. Cinématiques + carto v1 → ateliers → arbres → conclusions → restitution

## Archive
reglementations/euro-numerique/ : l'étude €N (GT8-GT10), référence de méthode et de format.

## Actions
Base Notion « Actions CRA » (Action, Porteur, Échéance, Statut, Lien CR), alimentée par /reunion,
lue par /brief. Pas d'intégration calendrier (tenant NDA verrouillé) : les métadonnées de séance
sont demandées dans /reunion.
